name: test

on:
  push:
    paths:
      - "skills/flow/scripts/**"
      - "skills/flow/templates/**"
      - "tests/**"
      - ".github/workflows/test.yml"
  pull_request:
    paths:
      - "skills/flow/scripts/**"
      - "skills/flow/templates/**"
      - "tests/**"
      - ".github/workflows/test.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash tests/guard.test.sh
      - run: bash tests/scripts.test.sh
